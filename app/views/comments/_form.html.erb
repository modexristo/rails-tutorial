<!--
    the form_for will build a nested route such as '/articles/1/comments'
    and by default uses POST, so POST /articles/1/comments => comments#create

    The @article inst. variable is available to all views and forms because it is
    defined as an instance variable.
-->
<%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.label :commenter %>
      <%= f.text_field :commenter %>
    </p>

    <p>
      <%= f.label :body %>
      <%= f.text_area :body %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>